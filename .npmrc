# Pin the @sitegpt scope to the PUBLIC npm registry for this repo.
# Without this, a user-level ~/.npmrc mapping (@sitegpt -> GitHub Packages)
# silently reroutes `npm publish` — which is exactly what happened on the
# first v0.1.0 publish attempt. This file wins over the user config.
@sitegpt:registry=https://registry.npmjs.org
